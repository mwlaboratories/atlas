# Private — the layout/build pipeline + webtool stays out of the public repo.
# Only the generated PCB artifacts under pcb/ are public-facing.
tools/
node_modules/

# per-user devenv state
.devenv/
.direnv/
devenv.local.nix

# Local Claude harness
.claude/
CLAUDE.md

# OS / editor
.DS_Store
*.swp
*~

# KiCad cruft (user-local, generated, or temp)
_autosave-*.kicad_pcb
~*.lck
*.kicad_prl
*.bak
fp-info-cache

# Fabrication-Toolkit output
/pcb/kicad/production/
/pcb/kicad/*.zip

# Generated 3D / fab outputs — re-generate via scripts when needed.
/pcb/kicad/*.step
/pcb/kicad/*.STEP
/keyboard-plate.stl
*.dxf
*.gbr
*.drl
gerbers/
