# Devenv / direnv
.devenv/
.direnv/
devenv.local.nix

# Local Claude harness
.claude/
CLAUDE.md

# OS / editor
.DS_Store
*.swp
*~

# Build / output (placeholders for later)
build/
out/

# KiCad cruft (user-local, generated, or temp)
_autosave-*.kicad_pcb
~*.lck
*.kicad_prl
*.bak

# Generated 3D / fab outputs — re-generate via scripts when needed.
# Vendored 3D models in **/3dmodels/ are NOT generated, keep them tracked.
/pcb/atlas/pcb*.step
/pcb/atlas/pcb*.STEP
/keyboard-plate.stl
*.dxf
*.gbr
*.drl
gerbers/
