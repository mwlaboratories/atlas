# Devenv / direnv
.devenv/
.direnv/
devenv.local.nix

# Local Claude harness
.claude/
CLAUDE.md

# OS / editor
.DS_Store
*.swp
*~

# Build / output (placeholders for later)
build/
out/

# KiCad cruft (user-local, generated, or temp)
_autosave-*.kicad_pcb
~*.lck
*.kicad_prl
*.bak

# Generated 3D / fab outputs — re-generate via scripts when needed.
# Vendored 3D models live in **/3dmodels/ subfolders and stay tracked.
/pcb/atlas/*.step
/pcb/atlas/*.STEP
!/pcb/atlas/3dmodels/
/keyboard-plate.stl
*.dxf
*.gbr
*.drl
gerbers/
